node-linker=hoisted
# Do not blanket-hoist all expo/* packages — that mixed SDK 54 + 57 at the root
# and broke Expo Go (wrong babel-preset / private fields on Hermes).
public-hoist-pattern[]=*react-native*
public-hoist-pattern[]=@react-native*
