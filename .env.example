# Copy to apps/api/.env.local and packages/db/.env as needed.
# Neon + Better Auth + Blob land in the next setup steps.

DATABASE_URL=
BETTER_AUTH_SECRET=
EXPO_PUBLIC_API_URL=http://localhost:3000
BLOB_READ_WRITE_TOKEN=
