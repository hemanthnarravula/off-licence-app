# Local / Vercel
DATABASE_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
BLOB_READ_WRITE_TOKEN=

# Optional: AI label extraction (Vercel AI Gateway). Without this, photo upload still works; fields stay manual.
AI_GATEWAY_API_KEY=
# Optional override (default: google/gemini-2.5-flash-lite)
# AI_GATEWAY_MODEL=google/gemini-2.5-flash-lite

# For local Postgres (dev only example):
# DATABASE_URL=postgresql://offlicence:offlicence@127.0.0.1:5432/offlicence
